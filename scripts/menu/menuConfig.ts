/** Runtime lunch schedule configuration for generated demo menus. */
export const MENU_RUNTIME_SCHEDULE = {
  /** First weekday served by the React app (Monday). */
  runtimeStartDate: '2026-07-27',
} as const;
