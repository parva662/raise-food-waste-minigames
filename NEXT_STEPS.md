# Next steps — GameBus integration

1. Confirm the final declaration data model (`mealChoice`, `regularMainSelected`, `regularVegetarianSelected`, bundled soup selections, scoring fields).
2. Design matching GameBus Property templates and JSON schemas.
3. Update the `studentLunchCheckin` Activity template.
4. Refresh the GameBus configuration export documents.
5. Connect **Submit** to one final ACTIVITY (single submission, no updates).
6. Verify that GameBus receives the activity and closes the iframe.
7. Import the real menu later (keep temporary `menuSchedule` / catalogue until then).
